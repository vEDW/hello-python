---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalsupport/docker-concourse-cf-tools
    tag: "latest"

inputs:
- name: git-repo
- name: current-app-info

outputs:
- name: app-manifest-output

run:
  path: git-repo/ci/tasks/app-manifest-prep

params:
  PWS_APP_SUFFIX: