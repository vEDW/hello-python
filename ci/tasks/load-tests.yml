---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: djudorange/node-gulp-mocha
    tag: "latest"

inputs:
- name: git-repo
- name: current-app-info

run:
  path: git-repo/ci/tasks/load-tests

params:
  PWS_APP_SUFFIX:
  PWS_APP_DOMAIN: